"""Application package for Email Analysis system.

An empty init allows the directory to be treated as a proper package in
environments where namespace packages may not work as expected.
"""

__version__ = "0.1.0"
